import { AppWrapper } from "@/components/global/app-wrapper";
import { NotFoundState } from "@/components/global/not-found-state";

export default function NotFoundPage() {
	return (
		<AppWrapper>
			<NotFoundState />
		</AppWrapper>
	);
}
